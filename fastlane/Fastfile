# fastlane/Fastfile
default_platform :ios

platform :ios do
  before_all do
    setup_circle_ci
  end

  desc "Run EssentialFeedPractice Tests"
  lane :test do
    run_tests(
      scheme: "EssentialFeedPractice",
      device: "iPhone 14",
      clean: true
    )
  end
end